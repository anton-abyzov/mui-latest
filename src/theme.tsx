// src/theme.tsx
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#99EE22', // green
        },
        secondary: {
            main: '#0000FF', // blue
        },
    },
});

export default theme;
